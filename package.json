{
  "name": "send-email",
  "version": "1.0.0",
  "description": "send email",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress": "npx cypress run --reporter mochawesome --reporter-options reportDir=\"/cypress/results\",overwrite=false,html=false,json=true && npx mochawesome-merge \"/cypress/results/*.json\" -o mochawesome.json && npx marge mochawesome.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fs": "^0.0.1-security",
    "node-mailer": "^0.1.1",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "archiver": "^6.0.1",
    "cypress": "^13.6.6",
    "mocha": "^10.3.0",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.3.0",
    "mochawesome-report-generator": "^6.2.0",
    "zip-a-folder": "^3.1.6"
  }
}
